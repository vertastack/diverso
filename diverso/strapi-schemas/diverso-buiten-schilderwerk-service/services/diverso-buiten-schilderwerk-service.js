"use strict";

/**
 * diverso-buiten-schilderwerk-service service
 */

const { createCoreService } = require("@strapi/strapi").factories;

module.exports = createCoreService(
  "api::diverso-buiten-schilderwerk-service.diverso-buiten-schilderwerk-service",
);
