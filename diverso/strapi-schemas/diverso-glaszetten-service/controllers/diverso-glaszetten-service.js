"use strict";

/**
 * diverso-glaszetten-service controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController(
  "api::diverso-glaszetten-service.diverso-glaszetten-service",
);
